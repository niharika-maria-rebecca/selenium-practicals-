package ex7sublogin;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class Login {
    public static void main(String[] args) {

        // Set path to chromedriver
        System.setProperty("webdriver.chrome.driver", "/home/user/Downloads/chromedriver");

        // Launch Chrome
        WebDriver driver = new ChromeDriver();

        // Open Google
        driver.get("https://www.google.com");

        // Click on "Sign in"
        driver.findElement(By.linkText("Sign in")).click();

        // Enter email
        driver.findElement(By.id("identifierId")).sendKeys("abc@gmail.com");

        // Example: Press ESCAPE key
        driver.findElement(By.id("identifierId")).sendKeys(Keys.ESCAPE);

        // Optional: Close browser
        // driver.quit();
    }
}
